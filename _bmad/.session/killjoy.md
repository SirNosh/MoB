# Killjoy Session Log

## [2026-04-18 19:55] round1-feasibility-audit-three-scales
- **artifact**: C:\MoB Final\docs\research-party\round1\killjoy-position.md
- **outcome**: Wrote ~2350-word feasibility audit of MoB auction at MNIST / CIFAR-100 / LLM-MoE scales. Quantified bid FLOPs, memory, and bandwidth vs. forward/backward costs at each scale. Key findings: (1) at MNIST the auction is a 5x tax dominated by EWC per-expert backward, not Mahalanobis — ignore; (2) at CIFAR-100 auction fits iff Fisher lives on LoRA adapter + head (frozen ViT backbone), tied covariance + whitened Euclidean gives 70x Mahalanobis speedup, K=2200 crossover; (3) at LLM scale full per-expert covariance (32 MB fp16/expert × 128 = 4 GB/layer × 60 layers = 240 GB) and K backward passes per step (128x training cost) are both infeasible. Proposed min-viable: tied low-rank r=32 Mahalanobis + projected-gradient EWC with shared backward. Dealbreaker: per-expert backward for forget cost forces mechanism change at Scale 3.
- **follow_up**: Astra round 2 should address whether tied covariance preserves auction theoretical properties; Chamber/Sage should validate projected-Fisher approximation against EWC guarantees; Fade should weigh in on whether bid-aware routing actually beats aux-loss-free balancing at 100B+ scale.

## [2026-04-18 20:35] round2-5to8B-recost-and-basemodel-ranking
- **artifact**: C:\MoB Final\docs\research-party\round2\killjoy-r2.md
- **outcome**: Re-costed auction at 5-8B scale cap across 4 candidates (OLMoE-1B/7B, Mistral-7B upcycled, Llama-3-8B upcycled, Phi-3.5-mini upcycled). OLMoE ranked #1: native MoE (16L×64K×d=2048), fits single A100/H100 80GB full FT, 2-5% throughput tax, bid-log 8MB/fwd at fp16. Mistral/Llama-3 upcycled requires 2×H100 (~90GB MoE params). Phi-3.5 upcycled #2. Resolved R1 cruxes: (1) tied U preserves DSIC but weakens no-starvation — add per-expert diagonal α_i (8KB/expert); (2) projected-Fisher safe at r_f≥16, ship r_f=32; (3) bid-as-interpretability-signal justifies mechanism vs aux-loss-free at scale (10000× richer per-token-per-expert signal). Priced interpretability: full training-run bid log = 25 TB infeasible, sample 1% → 250 GB tractable; inference side-channel at 262 MB/s uncompressed, 3 MB/s compressed+sampled; nearest-sample FAISS lookup over 10M-token eval set = 40 GB disk + 2 GB RAM. New dealbreaker: interpretability bid tensor must be detached from backward graph, else +128 MB activation memory OOMs at B=8 without grad checkpointing. Old per-expert-backward dealbreaker resolved by projected-Fisher.
- **follow_up**: Astra to confirm detaching bids from backward graph does not break mechanism incentive-compatibility; Chamber to validate r_f=32 projected-Fisher via ablation on LoRA Fisher tail distributions; Fade to run OLMoE-1B/7B upcycling feasibility experiment with 1% bid sampling to measure actual vs projected throughput tax.
