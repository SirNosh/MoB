# Chamber session log

## [2026-04-18 19:55] Round 1 position paper - three-scale MoB routing architecture
- **artifact**: C:\MoB Final\docs\research-party\round1\chamber-position.md
- **outcome**: Committed concrete architecture at three scales: S1 full CNN experts + L2-normed 64-d W_route projection head for prototype-collapse fix; S2 frozen ViT-B/16 + per-expert LoRA(r=8)+adapter, EWC on adapters only, per-expert W_route on shared CLS; S3 DeepSeek-style 64 routed + 2 shared FFN experts per layer, diagonal+tied-low-rank-U covariance via Woodbury, top-k per-token auction replacing Mixtral gate. Cross-scale invariant is a parameter-free `AuctionRouter` with fixed type signature whose buffers (mu, diag, U, fisher) update by EMA. Pseudocode with tensor shapes provided at every scale.
- **follow_up**: Await Astra/Sage adjudication on Crux 1 (is EWC Fisher right at LLM scale vs rehearsal loss / gradient norm / drift penalty); Fade/Sage on Crux 2 (fine-grained 64+2 required to avoid bid monopoly?); Killjoy on Crux 3 (single vs per-block auction inconsistency between S2 and S3). Dealbreaker = empirical proof that EMA-updated prototypes on losing-near-winner experts stale within 10k steps at Scale 3.

## [2026-04-19 14:30] Pre-Phase-2 commission — per-task epochs + Lipschitz L
- **artifact**: none (design spec delivered inline to Nosh)
- **outcome**: (Q1) Recommend 20 epochs/task AdamW + cosine, lr=5e-4 adapter / 1e-3 LoRA scale, bs=128, wd=1e-4, warmup=linear-100-steps on task-1 only; reset optimizer at every task boundary; backbone stays frozen (no LN exception); adapter-only trainable. Anchored to LAMDA-PILOT fecam.json (tuned_epoch=20, SGD, lr=1e-2, bs=48), EASE (20), APER-Adapter (20), RanPAC (20), MOS (20), CODA-Prompt (20); L2P/DualPrompt-5 rejected as prompt-only outliers. (Q2) Defined L in L2-normalized CLS feature space (diam(X)=2 exactly), three-stage estimation protocol (random pairs → nearest-neighbor → gradient-ascent adversarial) with N_pairs=10k on WinningSet ∪ GlobalBank, prior L ≈ 6–15 at S2, yielding γ_min ≈ 3–8 in normalized bid units — consistent with Sage §6.2. S1/S2/S3 cost ≈ 0.05/0.2/2 GPU-hr respectively; bundle into Jett's V₁/V₂ empirical-Fisher pass (§7 pilot).
- **follow_up**: Nosh to circulate Q1 spec to Jett for §8 frozen_config.yaml; Q2 protocol ready to hand to Cypher (data pair sampling) + Jett (instrumentation). Escalation: if pilot reveals L > 30 at S2, γ_min in normalized units exceeds α — would force α/γ rebalancing before freeze and is a v1.2 escalation-worthy event.

## [2026-04-18 20:20] Round 2 — OLMoE base, re-spec, interpretability, SALE contrast
- **artifact**: C:\MoB Final\docs\research-party\round2\chamber-r2.md
- **outcome**: Committed OLMoE-1B/7B as Scale 3 base (16 layers, d=2048, 64 experts native, no upcycling needed). Accepted Killjoy's projected-gradient EWC (r_f=32, one shared backward/step, 256 bytes/expert Fisher state) and Sage's DeepSeek-V3 bias conscience term. Kept tied low-rank r=32 + per-expert diagonal; total mechanism buffer = ~12.8 MB across all 16 MoE layers. Retracted S2/S3 single-vs-per-layer-auction inconsistency in favor of per-layer (route at granularity of feature aggregation). Added BidTrace instrumentation — zero-param logging hook emitting (token, layer, top-k experts, bid components) enabling per-expert specialization maps and prototype-to-nearest-token projections as first-class interpretability artifacts. Updated cross-scale invariant `AuctionRouter` signature adds optional trace_sink. New secondary dealbreaker: DeepSeek-V3 bias magnitude swamping mahal+forget within 1B Dolma tokens, reducing MoB to bias-routing-with-decorative-prototypes.
- **follow_up**: Hand forward-pass pseudocode to Jett for OLMoE integration (swap target: OlmoeSparseMoeBlock.gate). Ask Cypher/Killjoy to run the prototype-drift falsification on 8-expert synthetic before any OLMoE compute spend. Ask Astra whether projected-gradient EWC preserves the DSIC property under the projection basis.
